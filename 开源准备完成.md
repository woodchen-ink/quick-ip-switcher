# Quick IP Switcher - 开源准备完成 ✅

## 已完成的工作

### 1. ✅ 代码优化
- [x] 添加版本变量到 main.go,支持编译时注入版本号
- [x] 窗口标题显示版本信息
- [x] 完善注释和文档

### 2. ✅ GitHub Actions 自动构建
- [x] 创建 `.github/workflows/release.yml` 工作流
- [x] 触发条件: 推送版本标签 (v*)
- [x] 自动生成 Windows manifest
- [x] 编译可执行文件并注入版本号
- [x] 创建 ZIP 发布包(包含exe、文档、许可证)
- [x] 自动创建 GitHub Release

### 3. ✅ 文档完善
- [x] 更新中文 README.md,添加 GitHub badges
- [x] 创建英文 README_EN.md
- [x] 添加下载安装说明
- [x] 完善功能特性和使用示例
- [x] 创建 LICENSE 文件 (MIT)
- [x] 创建 .gitignore 文件
- [x] 创建 GITHUB_INFO.md 指南

### 4. ✅ 项目信息
- [x] 项目名称推荐: **quick-ip-switcher**
- [x] 项目描述(中英文)
- [x] GitHub Topics 标签
- [x] 发布流程说明

## 推荐项目名称

**quick-ip-switcher** ⭐

其他备选:
- network-config-manager
- ip-switcher-win
- windows-ip-switcher

## 项目描述

### 中文
快速IP切换工具 - 一个轻量级的Windows GUI工具,用于管理和一键切换网络适配器IP配置。支持多网卡、多配置管理,使用Windows原生API,无窗口弹出,配置持久化存储。

### English
Quick IP Switcher - A lightweight Windows GUI tool for managing and switching network adapter IP configurations with one click. Supports multiple adapters and configurations, uses Windows native API, no popup windows, with persistent configuration storage.

## 下一步操作

### 1. 创建 GitHub 仓库

```bash
# 初始化 Git
git init
git add .
git commit -m "Initial commit: Quick IP Switcher v2.1"

# 在 GitHub 上创建仓库 quick-ip-switcher
# 然后执行:
git remote add origin https://github.com/YOUR_USERNAME/quick-ip-switcher.git
git branch -M main
git push -u origin main
```

### 2. 发布第一个版本

```bash
# 创建版本标签
git tag -a v2.1.0 -m "Release v2.1.0 - Initial public release"

# 推送标签(会自动触发 GitHub Actions 构建)
git push origin v2.1.0
```

GitHub Actions 将自动:
1. ✅ 编译 quick-ip-switcher.exe (带版本号)
2. ✅ 创建 quick-ip-switcher-v2.1.0-windows-amd64.zip
3. ✅ 发布 GitHub Release
4. ✅ 附加编译好的文件

### 3. 配置 GitHub 仓库设置

在 GitHub 仓库页面:

**About 区域:**
- Description: 🔧 A lightweight Windows GUI tool for managing and switching network IP configurations | Windows 网络IP配置一键切换工具
- Topics: `windows` `go` `network-configuration` `ip-switcher` `network-manager` `windows-gui`

**别忘了:**
- 替换 README.md 中的 `YOUR_USERNAME` 为你的 GitHub 用户名

### 4. 后续版本发布

每次想发布新版本:

```bash
# 1. 修改代码
git add .
git commit -m "描述你的改动"

# 2. 推送到 main
git push

# 3. 创建新版本标签
git tag -a v2.1.1 -m "Release v2.1.1 - 改进描述"
git push origin v2.1.1
```

## 文件清单

✅ 核心代码:
- main.go (含版本变量)
- config.go
- network.go
- network_windows.go

✅ 文档:
- README.md (中文,含 badges)
- README_EN.md (英文)
- GITHUB_INFO.md (发布指南)
- 使用说明.txt
- LICENSE (MIT)

✅ 配置:
- .gitignore
- .github/workflows/release.yml (自动构建)
- go.mod
- build.bat

✅ 资源:
- rsrc.syso (Windows manifest,编译生成)

## 项目亮点

✨ **零依赖单文件** - 单个exe,无需安装
✨ **Windows原生API** - 不弹PowerShell窗口
✨ **配置持久化** - JSON配置自动保存
✨ **多网卡支持** - 管理多个网络适配器
✨ **一键切换** - 快速切换IP配置
✨ **自动构建** - GitHub Actions自动打包
✨ **开源免费** - MIT协议

## 建议的 GitHub Topics

```
windows
go
golang
network-configuration
ip-switcher
network-manager
windows-gui
system-utility
network-tools
winapi
```

## 补充建议

1. **添加截图**: 运行程序截图,放到 `docs/images/screenshot.png`,在 README 中展示
2. **创建 Wiki**: 添加更详细的使用教程和常见问题
3. **Issue 模板**: 创建 bug report 和 feature request 模板
4. **贡献指南**: 创建 CONTRIBUTING.md
5. **更新日志**: 创建 CHANGELOG.md

---

**准备就绪,可以开源了!** 🎉
